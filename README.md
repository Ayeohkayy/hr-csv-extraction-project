# hr-csv-extraction-project
Extracted synthetic HR CSV data into PostgreSQL using R, validated raw loads, and created SQL staging tables and views to prepare employee, compensation, and performance data for downstream analytics.
